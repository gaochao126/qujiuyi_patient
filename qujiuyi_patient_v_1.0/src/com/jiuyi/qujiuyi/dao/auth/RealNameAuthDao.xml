<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.jiuyi.qujiuyi.dao.auth.RealNameAuthDao">
	<insert id="createRealNameAuth" parameterType="RealNameAuthDto">
		insert into t_real_name_auth (userId, userType, realName, uid, authImage, createTime) 
		values(#{userId}, 2, #{realName}, #{uid}, #{authImage}, #{createTime})
	</insert>
	
	<select id="queryRealNameAuthingByUserId" parameterType="RealNameAuthDto" resultType="RealNameAuthDto">
		select * from t_real_name_auth
		<where>
			userId = #{userId} and authStatus = 0
		</where>
	</select>
</mapper>